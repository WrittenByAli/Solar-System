# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Production builds
dist/
build/

# Environment & secrets (never commit credentials)
.env
.env.*
!.env.example
!.env.sample
*.pem
*.p12
*.pfx
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*credentials*.json
**/google-services.json
**/serviceAccount*.json
**/client_secret*.json
secrets/
*.keystore

# Local scratch / personal files at repo root (not the web app)
/To do list.pdf
/*.pdf
/logo.png
/logo2.png
/solar.png
/extract.ps1
/extract2.ps1

# Local grid / export dumps (PDFs under 1/)
1/**/*.pdf

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage & test artifacts
coverage/
*.lcov
.nyc_output/
test-results/
verify_submit*

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Bundler caches
.cache/
.parcel-cache/

# Other framework outputs (not used but harmless)
.next/
.nuxt/

# Storybook build outputs
.out/
.storybook-out/

# Temporary folders
tmp/
temp/

# Editor directories and files
!.vscode/extensions.json
.history/*

# Cursor / local AI tooling (can contain paths or snippets you don’t want public)
.cursor/
**/.cursor/
.claude/

# Agent / transcript exports (if ever copied into the repo)
**/agent-transcripts/

# Deploy vendor local state
.netlify/
.vercel/

# TypeScript cache
*.tsbuildinfo

# Internal docs & security-sensitive files — kept local, not published.
# (CLAUDE.md and README.md remain tracked.)
*.md
!CLAUDE.md
!README.md
supabase_*.sql
qa-*.mjs
test-*.mjs
shot-*.mjs
.vercel
